#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    if key != value:
        return a_dictionary
    else:
        for i in range(len(a_dictionary[key]):
                del a_dictionary[i]
