#!/usr/bin/python3
def simple_delete(a_dictionary, key=""):
    if key does not exist:
        return a_dictionary
    else:
        for i in len(a_dictionary):
            if i = key:
                del i
            
